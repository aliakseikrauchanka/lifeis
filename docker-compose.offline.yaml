services:
  # Client App
  entry-app:
    extends:
      file: docker-compose.base.yml
      service: nx-app-base
    command: npx nx run entry-app:serve --host=0.0.0.0
    ports:
      - ${CLIENT_PORT}:4200
      # PORT for million lint
      # - 42423:42423
    environment:
      - VITE_MODE=offline
      - VITE_BE=${NGINX_DOMAIN}
      - VITE_CLIENT_ID=${CLIENT_ID}
      - VITE_FF_ENVIRONMENT=${FLAGSMITH_ENVIRONMENT}

  # Node.js Server App
  entry-server:
    extends:
      file: docker-compose.base.yml
      service: nx-app-base
    command: npx nx run entry-server:serve --host=0.0.0.0
    ports:
      - ${SERVER_PORT}:3000
    environment:
      - MODE=offline
      - PORT=3000
      - DB_URI=${DB_URI}
      - REDIRECT_URL=${NGINX_DOMAIN}
      - CLIENT_ID=${CLIENT_ID}
      - CLIENT_SECRET=${CLIENT_SECRET}
      - LOG_REDIRECT_URL=${LOG_REDIRECT_URL}
      - LOG_CLIENT_ID=${LOG_CLIENT_ID}
      - LOG_CLIENT_SECRET=${LOG_CLIENT_SECRET}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - NOTION_API_KEY=${NOTION_API_KEY}

  insights-app:
    extends:
      file: docker-compose.base.yml
      service: nx-app-base
    command: npx nx run insights-app:serve --host=0.0.0.0
    ports:
      - ${LOG_PORT}:4200
    environment:
      - VITE_BE=${NGINX_DOMAIN}
      - VITE_CLIENT_ID=${LOG_CLIENT_ID}

  logs-app:
    extends:
      file: docker-compose.base.yml
      service: nx-app-base
    command: npx nx run logs-app:serve --configuration=development
    ports:
      - ${LOGS_PORT}:4200

volumes:
  mono-node-modules: null
